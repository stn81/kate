#!/bin/bash
# kate_init 已废弃（tombstone，保留至 v1.2 后删除）。
# 模板已并入主模块（cmd/kate/template/service，随框架同仓同 tag 演进、CI 持续编译验证），
# 脚手架改为 Go CLI，无需 clone 本仓库：
echo "kate_init is deprecated and gone. Use instead:" >&2
echo "" >&2
echo "  go run github.com/stn81/kate/cmd/kate@latest new <module> [flags]" >&2
echo "" >&2
echo "  e.g. go run github.com/stn81/kate/cmd/kate@latest new dw-sync2" >&2
echo "  see: go run github.com/stn81/kate/cmd/kate@latest help" >&2
exit 1
